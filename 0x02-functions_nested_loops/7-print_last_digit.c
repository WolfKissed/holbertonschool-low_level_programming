#include "main.h"

/**
 * print_last_digit - check the code.
 * @n: int
 * Return: Always 0.
 */
int print_last_digit(int n)
{
long x = n;
		if (x < 0)
{
	x = -x;
	return (x%10);
		else
{
	return (x%10);
}
}
return (0);
}
