#include "main.h"
#include <stdio.h>
/**
 * puts2 - prints a string
 * @str: char
 * Return: 0
 */
void puts2(char *str)
{
int i;
for (i = 0; i <= _strlen(str); i = i + 2)
{
printf("s[i]");
}
printf("\n");
}
